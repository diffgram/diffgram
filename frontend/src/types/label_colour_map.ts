// Define a type alias for a map of label colors
export type LabelColourMap = {
  // The key is a number representing a label
  number: // The value is an InstanceColor object
    InstanceColor // Imported from the instance_color module
};
